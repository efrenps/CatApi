# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'cat-api' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cat-api
  pod 'Alamofire', '5.0.2'
  pod 'SDWebImage', '~> 5.0'
  pod 'Cosmos', '~> 23.0'
  
  target 'cat-apiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cat-apiUITests' do
    # Pods for testing
  end

end
